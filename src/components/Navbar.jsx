import React from "react";
import { Z_GREEN } from "../constants";

export default function Navbar({ onNavigate, page }) {
  return (
    <header className="flex items-center justify-between px-4 py-3 border-b bg-white/90 backdrop-blur">
     
    </header>
  );
}
